CHANGELOG.md

# Written by pnpm in its own format, and rewritten on every install. Checking it
# means any install leaves the format gate failing, and reformatting it produces
# a four-thousand-line diff that the next install throws away.
pnpm-lock.yaml

# Publish staging artifacts, generated by scripts/prepare-publish.mjs
apps/daemon/ui-dist/
apps/daemon/README.md
packages/*/LICENSE
apps/daemon/LICENSE
